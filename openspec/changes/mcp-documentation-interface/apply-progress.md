# Apply Progress: MCP documentation interface

## Slice 1b — Contracts and Node TDD harness

- Slice 1a completed task `1.0` at `0130e674d407a738b0fc0900232519c44ff96225`; Slice 1b completed tasks `1.1`–`1.5` without changing Slice 1a harness plumbing.
- Slice 1b deleted predecessor `tests/node/harness.test.ts` after replacing its tautological assertion with behavior-focused contract and bounds tests, as recorded in `tasks.md`.
- Added the versioned v1 bounds profile, strict index/request/response schemas, and an isolated TypeScript configuration for the Node contract layer.
- No dependencies, package metadata, Vitest configuration, or Playwright configuration changed in Slice 1b; Slice 1a already supplied the harness plumbing.

## Persisted task completion

- `1.0`, `1.1`, `1.2`, `1.3`, `1.4`, and `1.5` are visibly marked `- [x]` in `tasks.md`.
- Parent-owned lifecycle actions: none in this task artifact; all later implementation-owned slices remain deferred.

## TDD Cycle Evidence

| Cycle | Evidence |
|---|---|
| Safety net | Before corrective edits, `npm run test:node` passed 5/5. |
| Original RED/GREEN | The initial profile RED failed 2/2 because `config/docs-mcp/bounds.v1.json` was absent; the initial focused suite later passed 5/5. |
| Corrective RED | After new tests, `npm run test:node` failed 10/14: absent capacity/deadline behavior, accepted recognized names in `requested_operation`, and accepted undeclared bounds metadata. |
| Corrective GREEN | Added strict metadata parsing, operation-field correlation, and minimal capacity/deadline predicates; focused contract/bounds tests passed 14/14. |
| TRIANGULATE | Independently authored cases cover 64/65 sessions, 899/900 TTL, 299/300 SSE, 256/257 events, 29/30 synchronization, 9/10 reload, 29/30 startup, and 3/4 retention, plus empty metadata/record identities. |
| REFACTOR | No further refactor was needed after the smallest corrective schema and predicate changes. |

## Verification evidence

| Command | Outcome |
|---|---|
| `npm run test:node -- tests/node/docs-mcp/bounds.test.ts` | RED: 2 failed because the bounds config was absent. |
| `npm run test:node` (corrective RED) | FAIL: 10 failures out of 14 tests, as described above. |
| `npm run test:node -- tests/node/docs-mcp/contracts.test.ts tests/node/docs-mcp/bounds.test.ts` | PASS: 14 tests in 2 files. |
| `npm run test:node` (corrective GREEN) | PASS: 14 tests in 2 files. |
| `npm run check` | PASS: 0 errors, 0 warnings, 0 hints. |
| `npx playwright test --list` | PASS: 28 browser tests in `tests/docs.spec.ts`; no Node `*.test.ts` collection. |
| `npx playwright test` | Prior Slice 1b evidence: PASS: 28 tests; not rerun during this correction. |

## Changed files and rollback

- Added `config/docs-mcp/bounds.v1.json`, `tsconfig.docs-mcp.json`, `src/docs-mcp/contracts/{bounds,index,operations}.ts`, and focused Node contract/bounds tests.
- Deleted `tests/node/harness.test.ts` because it asserted only that Node exists.
- Updated this progress artifact and task text to record the Slice 1a/1b split and explicit harness deletion authorization; no browser behavior, Astro source, package dependency, or deployment topology changed.
- Rollback boundary: remove only the Slice 1b bounds/contracts/config/tests and restore the predecessor harness only with its Slice 1a harness rollback; static routes and browser search remain untouched.

## Status consumed

- Parent native status: `nextRecommended: apply`; `dependencies.apply: ready`; artifact store: `openspec`; repository edit authority granted.
- Action context warning: implementation is limited to Slice 1b allowed edit surfaces, uses the parent-held runtime attempt, and must not commit, push, review, or alter branch topology.
- Workload boundary: `auto-chain` with `feature-branch-chain`; current work unit is Slice 1b contracts on `feat/mcp-docs-contracts-v1`; complete candidate is 348 additions + 12 deletions = 360 authored lines, below 400.

## Design deviations

- None. The contract layer uses the existing direct `zod` dependency installed by Slice 1a and does not add dependencies.

## Remaining unchecked implementation tasks

- [ ] Measure WU0 only after all source-mutating planning normalization and immediately before candidate freeze, using the complete intended path set of modified `openspec/config.yaml` plus all files under `openspec/changes/mcp-documentation-interface/`; record the exact additions + deletions, frozen candidate identity, and `size:exception` binding without treating the provisional 2,896 observation as exact or authorizing commit, push, or PR. <!-- sdd-owner: implementation -->
- [ ] Validate the frozen planning baseline structurally by reading back both exact path roots, confirming the OpenSpec files and Engram tasks aggregate are content-equivalent, and recording that the unit changes no executable or source behavior. <!-- sdd-owner: implementation -->
- [ ] Confirm completion only after separately authorized ordinary-repository-policy commit and push/delivery steps establish the tracker baseline with all intended OpenSpec paths tracked, the exact baseline commit SHA verified locally and remotely, and the Slice 1 child branch created from that exact SHA; tasks do not authorize those steps, and `sdd-attempt acquire`, RDD START, and Slice 1 RED MUST NOT begin before that exact base exists. <!-- sdd-owner: implementation -->
- [ ] Keep the planning exception separate from executable review accounting: bind `size:exception` only to the frozen WU0 identity/count, require a new measurement/freeze after any post-freeze byte, path, or mode mutation, never apply it to a product slice, and retain no task-level authorization for commit, push, PR creation, merge, or deployment. <!-- sdd-owner: implementation -->
- [ ] **2.0 Mechanical prerequisite — After obtaining the exact dependency-plus-lockfile forecast from the real Slice 1 predecessor, add only `cheerio` if the RED fixture cannot load without it; prove the package/lockfile-only change contains no parser or publication behavior before running RED.** <!-- sdd-owner: implementation -->
- [ ] **2.1 RED — Author failing extraction/parity behavior tests** in `tests/node/docs-mcp/extract.test.ts` with English/Spanish built-page fixtures under `tests/node/docs-mcp/fixtures/extraction/` covering one H2, independent H3s, document ordinals, parent relationships, missing/extra/reordered IDs, duplicate or missing IDs, leading H3s, unsupported headings, malformed boundaries, and identical repeat input; tests MUST fail before `extract.ts` and `parity.ts` exist. <!-- sdd-owner: implementation -->
- [ ] **2.2 GREEN — Implement the smallest deterministic parser and parity validator** in `src/docs-mcp/indexer/extract.ts` and `src/docs-mcp/indexer/parity.ts`, reading only `main#contenido` from built `dist/index.html` and `dist/es/index.html`, forming exact H2/H3 ranges, ordinals, `parent_id`, and hierarchy, and comparing complete `(id, level, parent_id, ordinal)` tuples. <!-- sdd-owner: implementation -->
- [ ] **2.3 TRIANGULATE — Add parser and publication-blocking edge cases** to `tests/node/docs-mcp/extract.test.ts` for locale isolation, extra/missing/reordered IDs, hierarchy drift, duplicate IDs, malformed HTML, byte ceilings, ambiguous boundaries, and no partial record result after failure; retain deterministic error codes/messages bounded by Slice 1 contracts. <!-- sdd-owner: implementation -->
- [ ] **2.4 REFACTOR — Isolate traversal and parity diagnostics** in `src/docs-mcp/indexer/extract.ts` and `src/docs-mcp/indexer/parity.ts` while preserving exact tuple order, failure precedence, and fixture expectations. <!-- sdd-owner: implementation -->
- [ ] **2.5 Verify and freeze Slice 2** with focused extraction tests, `npm run test:node`, `npm run check`, and a clean `npm run build` check against current Astro output; normalize before freeze and ensure no generated `dist/` file is treated as authored source. <!-- sdd-owner: implementation -->
- [ ] **3.0 Mechanical prerequisite — After obtaining the exact dependency-plus-lockfile forecast from the real Slice 2 predecessor, add only `sanitize-html`, `turndown`, `turndown-plugin-gfm`, and `json-canonicalize` if required to execute the RED fixtures; prove package/lockfile-only setup has no conversion, identity, or publication behavior before RED.** <!-- sdd-owner: implementation -->
- [ ] **3.1 RED — Add failing rich-fidelity, identity, and publication tests** in `tests/node/docs-mcp/content.test.ts`, `tests/node/docs-mcp/identity.test.ts`, and `tests/node/docs-mcp/generate.test.ts` with fixtures under `tests/node/docs-mcp/fixtures/content/` for relative/fragment links, safe/unsafe URL schemes, scripts, styles, event handlers, forms, Mermaid, code, lists, tables, definitions, malformed markup, exact body/index limits, canonical URLs, and no-independent-copy input; tests MUST fail before conversion and generation exist. <!-- sdd-owner: implementation -->
- [ ] **3.2 GREEN — Implement one conversion pipeline** in `src/docs-mcp/indexer/content.ts` and `src/docs-mcp/indexer/identity.ts` using fixed sanitization, safe URL resolution, inert Mermaid fences, GFM Markdown, LF normalization, canonical JSON, and domain-separated SHA-256 identities; populate complete section/source/index/bounds metadata without LLM-generated text. <!-- sdd-owner: implementation -->
- [ ] **3.3 GREEN — Implement bounded staged generation and the post-build CLI** in `src/docs-mcp/indexer/generate.ts` and `scripts/docs-mcp/generate-index.ts`, consuming `config/docs-mcp/bounds.v1.json`, validating both locale pages and all records before writing only `dist/.well-known/gentle-ai/docs-mcp/manifest.v1.json` and `indexes/<index_identity>.json`, with no timestamps or hand-authored content. <!-- sdd-owner: implementation -->
- [ ] **3.4 TRIANGULATE — Prove fidelity, reproducibility, parity propagation, and fail-closed publication** in the three focused test files: repeat generation byte equality, source/index identity consistency, complete rich structure, invalid URL removal, unsafe content removal, oversized record/index output, malformed extraction, missing bounds, no stale/partial manifest, and manifest hash/size correctness. <!-- sdd-owner: implementation -->
- [ ] **3.5 REFACTOR — Keep conversion, identity, and staging responsibilities separate** in `src/docs-mcp/indexer/` and `scripts/docs-mcp/generate-index.ts`, preserving exact generated bytes and the rule that `dist/` is generated and never edited manually. <!-- sdd-owner: implementation -->
- [ ] **3.6 Verify and freeze Slice 3** with `npm run test:node -- tests/node/docs-mcp/content.test.ts tests/node/docs-mcp/identity.test.ts tests/node/docs-mcp/generate.test.ts`, `npm run build`, `npm run build:docs-mcp`, `npm run check`, and `npx playwright test`; normalize before candidate freeze and record generated artifact identity separately from authored line count. <!-- sdd-owner: implementation -->
- [ ] **4.1 RED — Author failing core tests** in `tests/node/docs-mcp/core-list.test.ts`, `tests/node/docs-mcp/core-get.test.ts`, and `tests/node/docs-mcp/core-errors.test.ts` for immutable snapshot capture, loaded/unavailable states, locale-only ordered listing, exact H2/H3 lookup, default Markdown, explicit HTML, descendants false/true, H3 empty descendants, unknown IDs, malformed/missing indexes, and mutually exclusive envelopes. <!-- sdd-owner: implementation -->
- [ ] **4.2 GREEN — Implement immutable `IndexStore` and ordered list behavior** in `src/docs-mcp/core/index-store.ts` and `src/docs-mcp/core/list.ts`, deep-freezing validated snapshots, swapping by reference only after complete validation, and returning locale-filtered ordinal order with shared loaded identities. <!-- sdd-owner: implementation -->
- [ ] **4.3 GREEN — Implement exact retrieval and closed dispatch** in `src/docs-mcp/core/get.ts` and `src/docs-mcp/core/dispatch.ts`, using `(locale, section_id)` map access, Markdown default, explicit sanitized HTML, no fuzzy/parent/sibling/locale fallback, optional ordered descendants, and exact failure envelope projection. <!-- sdd-owner: implementation -->
- [ ] **4.4 TRIANGULATE — Add identity and failure negatives** in `tests/node/docs-mcp/core-errors.test.ts` for unsupported locales, invalid identifiers/formats/options, unknown operations, undeclared fields, loaded-index identity retention, unavailable-index null identities, malformed-index classification, bounded response preflight, and evidence-only/no mutation behavior. <!-- sdd-owner: implementation -->
- [ ] **4.5 REFACTOR — Reduce duplication across list/get/dispatch** while preserving operation names, response shapes, immutable snapshot consistency, record locale fields, and the no-command/no-LLM/no-private-content boundary in `src/docs-mcp/core/`. <!-- sdd-owner: implementation -->
- [ ] **4.6 Verify and freeze Slice 4** with focused core tests, `npm run test:node`, `npm run check`, `npm run build`, and a read-only dependency/source audit for `src/docs-mcp/core/`; normalize before freezing the exact candidate. <!-- sdd-owner: implementation -->
- [ ] **5.1 RED — Add failing search and response-bound tests** in `tests/node/docs-mcp/core-search.test.ts` and `tests/node/docs-mcp/response-bounds.test.ts` for trimmed empty queries, accents, locale isolation, rank tiers, ordinal ties, snippet boundaries, default/20/over-20 limits, oversized section/serialized evidence, and complete response overflow. <!-- sdd-owner: implementation -->
- [ ] **5.2 GREEN — Implement deterministic search** in `src/docs-mcp/core/search.ts` with NFKD/combining-mark removal, stable title-prefix/title-substring/body ranking, immutable ordinal ties, original-text offset mapping, bounded first-match snippets, and validated default 8/max 20 behavior. <!-- sdd-owner: implementation -->
- [ ] **5.3 GREEN — Implement complete response preflight** in `src/docs-mcp/core/bounds.ts` and integrate it with `src/docs-mcp/core/dispatch.ts` so evidence, serialized operation results, error messages, and final responses fail with `bounded_response` rather than truncate or fabricate. <!-- sdd-owner: implementation -->
- [ ] **5.4 TRIANGULATE — Exercise independently authored Unicode and boundary cases** in the focused tests for accented Spanish terms, combining characters, first/last match windows, ellipses counted in the snippet bound, 8/20 result edges, all declared body/evidence/response bounds, and loaded/unavailable error identity retention. <!-- sdd-owner: implementation -->
- [ ] **5.5 REFACTOR — Separate normalization, ranking, snippet mapping, and serialization checks** without changing deterministic output or browser `src/scripts/site.js` behavior. <!-- sdd-owner: implementation -->
- [ ] **5.6 Verify and freeze Slice 5** with focused search/bounds tests, `npm run test:node`, `npm run check`, `npm run build`, and a source readback confirming browser search remains outside `src/docs-mcp/core/`; normalize before freeze. <!-- sdd-owner: implementation -->
- [ ] **6.0 Mechanical prerequisite — After obtaining the exact dependency-plus-lockfile forecast from the real Slice 5 predecessor, add only the pinned official `@modelcontextprotocol/server` v2 package if the protocol RED tests cannot load without it; prove the setup contains no SDK adapter or transport behavior before RED.** <!-- sdd-owner: implementation -->
- [ ] **6.1 RED — Add failing SDK protocol tests** in `tests/node/docs-mcp/mcp-transport.test.ts` and `tests/node/docs-mcp/mcp-legacy.test.ts` using protocol fixtures for exact three-tool advertisement, known/unknown calls, JSON and SSE, GET and POST, unsupported methods, JSON-RPC pre-dispatch errors, and legacy/modern handshake expectations. <!-- sdd-owner: implementation -->
- [ ] **6.2 GREEN — Implement the smallest SDK v2 adapter** in `src/docs-mcp/server/mcp.ts`, registering only `list_documentation_sections`, `search_documentation`, and `get_documentation_section`, routing all calls through `src/docs-mcp/core/dispatch.ts`, returning byte-equivalent structured/text JSON, and retaining documented legacy negotiation without a separate deprecated endpoint. <!-- sdd-owner: implementation -->
- [ ] **6.3 TRIANGULATE — Cover protocol and failure edges** in the transport tests for bounded unknown operation names, recognized operation failures, `id: null` pre-dispatch errors, method/content-type rejection, JSON/SSE selection, event/response bounds, read-only calls, and absence of undocumented rate-limit promises. <!-- sdd-owner: implementation -->
- [ ] **6.4 REFACTOR — Isolate SDK wiring from core contracts** in `src/docs-mcp/server/mcp.ts` and protocol fixtures while keeping the official package/version lock explicit and the legacy path enabled. <!-- sdd-owner: implementation -->
- [ ] **6.5 Verify and freeze Slice 6** with focused transport and legacy tests, `npm run test:node`, `npm run check`, `npm run build`, and `npx playwright test`; normalize before freeze and record the observed protocol revisions rather than assuming all clients use modern MCP. <!-- sdd-owner: implementation -->
- [ ] **7.1 RED — Author failing security and bounded-transport tests** in `tests/node/docs-mcp/security.test.ts`, `tests/node/docs-mcp/http-bounds.test.ts`, and `tests/node/docs-mcp/session-rate.test.ts` for allowed/disallowed/missing Origin, wildcard/reflection rejection, methods, content types, body bytes, direct/proxy IP derivation, spoofed forwarding headers, session 64/65, TTL 899/900, SSE 299/300, events 256/257, rate windows, and absent rate-limit headers. <!-- sdd-owner: implementation -->
- [ ] **7.2 GREEN — Implement Origin, method, content-type, and verified-IP guards** in `src/docs-mcp/server/security.ts` and `src/docs-mcp/server/http.ts`, with exact canonical allowlisting, explicit native-client missing-Origin policy, direct-TLS/trusted-proxy modes, no arbitrary forwarding-header trust, and deterministic pre-dispatch failure mapping. <!-- sdd-owner: implementation -->
- [ ] **7.3 GREEN — Implement finite session/SSE/event and body/response limits** in `src/docs-mcp/server/session-store.ts` and `src/docs-mcp/server/http.ts`, rejecting before allocation or parsing, closing bounded streams, and using `src/docs-mcp/core/bounds.ts` for complete-response enforcement. <!-- sdd-owner: implementation -->
- [ ] **7.4 GREEN — Implement application-owned fixed-window IP limiting** in `src/docs-mcp/server/rate-limit.ts` with fake-clock expiry, finite table capacity, verified client IP, deterministic `rate_limited`, and no API key/OAuth dependency or undocumented rate headers. <!-- sdd-owner: implementation -->
- [ ] **7.5 TRIANGULATE and REFACTOR — Add adversarial negatives and simplify guard order** in all three security tests and server modules, proving the specified failure precedence, no capability dispatch after rejection, no query/body/IP/origin leakage in responses/log hooks, and preserved read-only behavior. <!-- sdd-owner: implementation -->
- [ ] **7.6 Verify and freeze Slice 7** with focused security tests, `npm run test:node`, `npm run check`, `npm run build`, and `npx playwright test`; normalize source-mutating code before freezing and perform only check-only commands afterward. <!-- sdd-owner: implementation -->
- [ ] **8.0 Mechanical prerequisite — After obtaining the exact dependency-plus-lockfile forecast from the real Slice 7 predecessor, add only pinned `tsx` runner plumbing if the synchronization RED CLI cannot execute without it; prove package/lockfile-only setup has no fetch, validation, staging, or activation behavior before RED.** <!-- sdd-owner: implementation -->
- [ ] **8.1 RED — Author failing synchronization and activation tests** in `tests/node/docs-mcp/sync.test.ts` and `tests/node/docs-mcp/activation.test.ts` with temporary stores and fake clocks for bounded manifest/index downloads, zero redirects, origin containment, interrupted staging, bad hash/schema/version/bounds, 29/30-second timeout, atomic activation, 3/4 retention, stale/refresh-unknown, 9/10 reload, and preservation of the active snapshot. <!-- sdd-owner: implementation -->
- [ ] **8.2 GREEN — Implement bounded fetch, validation, staging, and version storage** in `src/docs-mcp/sync/synchronize.ts` and `src/docs-mcp/sync/version-store.ts`, using a fixed configured HTTPS origin, manifest-relative path containment, exact bytes/hashes/identities, exclusive lock, fsync/close, same-filesystem rename, and retained immutable versions. <!-- sdd-owner: implementation -->
- [ ] **8.3 GREEN — Implement atomic activation and one-shot CLI** in `src/docs-mcp/sync/version-store.ts` and `scripts/docs-mcp/sync-index.ts`, replacing `current` by atomic pointer swap only after validation, setting `previous`, notifying/reloading the service, and restoring the prior pointer when reload fails. <!-- sdd-owner: implementation -->
- [ ] **8.4 TRIANGULATE — Prove failure containment and identity freshness** in the synchronization tests for partial files, timeout at exact edges, stale known desired identity, refresh unknown with LKG service, no local valid index, invalid replacement isolation, version retention, and no static-site mutation. <!-- sdd-owner: implementation -->
- [ ] **8.5 REFACTOR — Separate network validation, filesystem transaction, and activation policy** while preserving atomicity and host neutrality; do not add systemd, PM2, nginx, cPanel, or provider-specific transfer assumptions to `src/docs-mcp/sync/` or `scripts/docs-mcp/sync-index.ts`. <!-- sdd-owner: implementation -->
- [ ] **8.6 Verify and freeze Slice 8** with focused sync/activation tests, `npm run test:node`, `npm run check`, `npm run build`, and a readback of temporary-store rollback behavior; normalize before freeze and retain only check-only commands afterward. <!-- sdd-owner: implementation -->
- [ ] **9.0 Mechanical prerequisite — After obtaining the exact dependency-plus-lockfile forecast from the real Slice 8 predecessor, add only pinned `pino` and `prom-client` packages if the operations RED seams cannot load without them; prove package/lockfile-only setup has no startup, health, logging, metrics, or recovery behavior before RED.** <!-- sdd-owner: implementation -->
- [ ] **9.1 RED — Author failing operations, health, and recovery tests** in `tests/node/docs-mcp/operations.test.ts`, `tests/node/docs-mcp/health.test.ts`, and `tests/node/docs-mcp/recovery.test.ts` for invalid config before listener binding, Node floor, 29/30-second startup, valid-index self-check of exactly three tools plus one retrieval, live/unready states, bounded failure categories, redacted logs/metrics, shutdown drain, and reproducible restore/regeneration. <!-- sdd-owner: implementation -->
- [ ] **9.2 GREEN — Implement startup, readiness, health, and shutdown** in `src/docs-mcp/server/main.ts`, including complete bounds/transport/index validation before network or filesystem mutation, `/health/live`, `/health/ready`, closed unready reasons, finite drain, and independent static-site operation. <!-- sdd-owner: implementation -->
- [ ] **9.3 GREEN — Implement bounded observability and host-neutral runbooks** in `src/docs-mcp/server/observability.ts`, `ops/docs-mcp/README.md`, and `ops/docs-mcp/recovery.md`, excluding queries, bodies, raw IPs, origins, paths, secrets, private content, and unbounded labels while documenting start/stop/restart/status interfaces abstractly. <!-- sdd-owner: implementation -->
- [ ] **9.4 TRIANGULATE — Verify operations failure categories and recovery** in the focused tests for missing/malformed/stale index, bad bounds, process failure, request rejection, sync failure, startup/reload deadline, graceful shutdown, backup restore, reproducible rebuild, readiness recovery, and unchanged `/` and `/es/` publication. <!-- sdd-owner: implementation -->
- [ ] **9.5 REFACTOR — Keep confirmed-environment adapters isolated** under `ops/docs-mcp/supervision/<confirmed-adapter>/` and document unresolved HostGator values as prerequisites rather than invented defaults; preserve the compiled service's host-neutral configuration enum. <!-- sdd-owner: implementation -->
- [ ] **9.6 Verify and freeze Slice 9 locally** with focused operation/health/recovery tests, `npm run test:node`, `npm run check`, `npm run build`, and `npx playwright test`; normalize before freeze and do not mark production ready from local evidence alone. <!-- sdd-owner: implementation -->
- [ ] **10.1 RED — Add failing evidence-ledger and qualification tests** in `tests/node/docs-mcp/client-evidence.test.ts` with fixtures under `tests/smoke/docs-mcp/` for missing/failed/unavailable runtime evidence, direct Claude/OpenCode/Codex claims, adapter-mediated Pi evidence, protocol-era scoping, public HTTPS/no-credentials guidance, and secret/path redaction. <!-- sdd-owner: implementation -->
- [ ] **10.2 GREEN — Implement smoke drivers and evidence validation** under `tests/smoke/docs-mcp/` and the ledger schema in `docs/mcp-client-compatibility.md`, requiring a real connection and successful read-only call for affirmative claims, recording only observed protocol behavior, and labeling Pi exactly as `Pi through pi-mcp-adapter`. <!-- sdd-owner: implementation -->
- [ ] **10.3 GREEN — Write setup and qualification guidance** in `ops/docs-mcp/client-smoke.md` and `docs/mcp-client-compatibility.md` for documented remote configurations, public HTTPS without API keys/OAuth, legacy compatibility qualification, tested versions/dates/transports, and plainly unverified workflows; do not claim native Pi MCP or universal modern-protocol support. <!-- sdd-owner: implementation -->
- [ ] **10.4 TRIANGULATE — Exercise evidence negatives and documentation checks** in `tests/node/docs-mcp/client-evidence.test.ts` for fixture-only non-proof, stale/missing versions, untested clients, unsupported protocol claims, adapter/native conflation, secrets, and undocumented rate-limit headers. <!-- sdd-owner: implementation -->
- [ ] **10.5 REFACTOR — Keep smoke execution, evidence normalization, and documentation projection separate** while preserving candidate endpoint/index identity binding and the rule that external evidence cannot be fabricated from fixtures. <!-- sdd-owner: implementation -->
- [ ] **10.6 Verify and freeze Slice 10** with focused evidence tests and the applicable smoke command against a confirmed endpoint; run `npm run test:node`, `npm run check`, `npm run build`, and `npx playwright test` as check-only evidence after normalization/freeze. <!-- sdd-owner: implementation -->
- [ ] **11.1 RED — Add failing integration and preservation tests** in `tests/node/docs-mcp/integration.test.ts`, `tests/node/docs-mcp/rollback.test.ts`, and `tests/docs.spec.ts` for generated manifest/index reachability, unchanged browser search contract, English/Spanish routes/canonicals/locale controls/Mermaid/sitemap, MCP disablement without site outage, index rollback without Astro rewrite, and first-slice exclusions. <!-- sdd-owner: implementation -->
- [ ] **11.2 GREEN — Wire final build/publication and regression assertions** in `package.json`, `netlify.toml`, `playwright.config.ts`, `tests/docs.spec.ts`, and the narrowest existing user-facing documentation entry point, preserving `npm run build`, `npx playwright test`, browser-only test collection, independent `src/scripts/site.js`, and static publication when MCP is unavailable. <!-- sdd-owner: implementation -->
- [ ] **11.3 TRIANGULATE — Run cross-slice negative and rollback cases** in `tests/node/docs-mcp/integration.test.ts` and `tests/node/docs-mcp/rollback.test.ts` for malformed/oversized index, unavailable service, rejected security request, stale synchronization, disabled runtime, no WebMCP/Edge/LLM/repository/auth/mutation path, and source/browser behavior remaining unchanged. <!-- sdd-owner: implementation -->
- [ ] **11.4 REFACTOR — Remove accidental coupling and redundant assertions** across final wiring and tests without changing the established Astro routes, browser search implementation, generated index authority, or independent rollback boundaries. <!-- sdd-owner: implementation -->
- [ ] **11.5 Verify and freeze the complete local candidate** after source-mutating normalization with `npm run test:node`, `npm run check`, `npm run build`, `npm run build:docs-mcp`, and `npx playwright test`; record authored additions/deletions per chain unit and confirm every post-freeze command was check-only. <!-- sdd-owner: implementation -->
- [ ] Confirm `HG-MCP-TLS-01` for the purchased HostGator plan: DNS, certificate issuance/renewal, public ports/firewall, HTTPS termination, and the trusted client-IP contract; if Apache reverse proxy is not confirmed, obtain `HG-MCP-TLS-FALLBACK-01` for direct Node HTTPS instead. <!-- sdd-owner: parent -->
- [ ] Confirm `HG-MCP-SUPERVISOR-01`: the actual purchased environment's start, stop, restart, status, boot-start, exit visibility, configuration injection, graceful termination, and log capture capability; do not assume systemd, PM2, cPanel, or a distribution. <!-- sdd-owner: parent -->
- [ ] Confirm `HG-MCP-CAPACITY-01`: actual Node 20+ runtime, CPU/memory/disk/inode limits, one-instance assumption, public ports, load behavior, and the finite bounds/rate policy under an operational probe. <!-- sdd-owner: parent -->
- [ ] Confirm `HG-MCP-RECOVERY-01`: off-server backup or reproducible recovery of release/configuration/bounds/transport/known-compatible index, followed by startup, readiness, one exact retrieval, and proof that static documentation remains online. <!-- sdd-owner: parent -->
- [ ] Do not activate the official public production route, publish host-specific supervisor/TLS instructions, or claim production readiness until all four HostGator gates have recorded evidence and the corresponding candidate has been reviewed. <!-- sdd-owner: parent -->
- [ ] Execute the four named-client runtime smokes against the candidate public HTTPS endpoint when the endpoint and client environments are available; retain failed/unavailable records as qualified evidence and do not infer PASS from fixtures. <!-- sdd-owner: parent -->
- [ ] Start or reuse bounded review for each exact normalized immutable slice candidate, preserving its branch, paths, modes, source/index identities, and review budget; RDD review supplies evidence only and never grants delivery authority. <!-- sdd-owner: parent -->
- [ ] After each accepted slice, verify the immediate-predecessor feature-branch target, clean focused diff, rollback boundary, recorded test evidence, and chain dependency diagram before allowing the next slice. <!-- sdd-owner: parent -->
- [ ] Before delivery/archive, validate the final native receipt/gate against the exact owner-issued candidate and confirm no post-freeze source mutation occurred; ordinary repository policy remains the delivery authority. <!-- sdd-owner: parent -->
